{% block body %}
        <div class="col-sm-12">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-lg btn-primary btn-block" type="submit">
                    Register
                </button>
            {{ form_end(form) }}
        </div>
{% endblock %}